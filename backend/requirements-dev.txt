# Test & development dependencies (kept separate from runtime requirements.txt
# so production images don't ship test tooling). Install with:
#   pip install -r requirements.txt -r requirements-dev.txt
pytest==8.3.4
pytest-asyncio==0.25.2
httpx==0.28.1
